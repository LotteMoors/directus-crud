import {render} from "@testing-library/react";
import Button from "../Button";

test("Render Button", () => {
  render(<Button label="Test" />);
});

test("Render Button test onclick actie", () => {
  render(<Button label="Test" />);
});
